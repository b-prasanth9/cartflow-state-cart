const cartId='demo-user';const products=[['Wireless Headphones',1499,'https://images.unsplash.com/photo-1505740420928-5e560c06d30e?auto=format&fit=crop&w=700&q=80'],['Smart Watch',2499,'https://images.unsplash.com/photo-1523275335684-37898b6baf30?auto=format&fit=crop&w=700&q=80'],['Running Shoes',2999,'https://images.unsplash.com/photo-1542291026-7eec264c27ff?auto=format&fit=crop&w=700&q=80']];
document.getElementById('products').innerHTML=products.map((p,i)=>`<article class="card"><img src="${p[2]}"><h3>${p[0]}</h3><p>₹${p[1]}</p><button onclick="add(${i})">Add to Cart</button></article>`).join('');
async function add(i){await fetch(`/api/cart/${cartId}/items`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({productName:products[i][0],price:products[i][1],quantity:1,imageUrl:products[i][2]})});load()}
async function load(){const r=await fetch(`/api/cart/${cartId}`);const items=await r.json();document.getElementById('cart').innerHTML=items.length?items.map(x=>`<div class="row"><span>${x.productName} × ${x.quantity}</span><span>₹${(x.price*x.quantity).toFixed(2)} <button onclick="update(${x.id},${x.quantity+1})">+</button><button onclick="update(${x.id},${Math.max(1,x.quantity-1)})">−</button><button onclick="removeItem(${x.id})">Remove</button></span></div>`).join(''):'<p>Your cart is empty.</p>';document.getElementById('total').textContent=items.reduce((s,x)=>s+x.price*x.quantity,0).toFixed(2)}
async function update(id,q){await fetch(`/api/cart/${cartId}/items/${id}?quantity=${q}`,{method:'PUT'});load()}async function removeItem(id){await fetch(`/api/cart/${cartId}/items/${id}`,{method:'DELETE'});load()}async function clearCart(){await fetch(`/api/cart/${cartId}/clear`,{method:'DELETE'});load()}load();